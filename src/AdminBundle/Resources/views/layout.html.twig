{% for message in app.session.flashBag.get('success') %}{{ message }}{% endfor %}
{% for message in app.session.flashBag.get('failed') %}{{ message }}{% endfor %}
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="{{ asset("bundles/myshop/css/mystyle.css") }}" rel="stylesheet" type="text/css" media="all"/>

    <title>Document</title>
</head>
<body>

<table>
    <tr>
        <td valign="top"  class="menu-td">
            <ul>
                <li><a href="{{ path("admin_homepage") }}">Главная</a></li>
                <li><a href="{{ path("admin.product.list") }}">Все товары</a></li>
                <li><a href="{{ path("admin.product.categorylist") }}">Товары по категориям</a></li>
            </ul>

            <ul>
                <li><a href="{{ path("admin.product.add") }}">Добавить товар</a></li>
                <li><a href="{{ path("admin.category.list") }}">Категории</a></li>
                <li><a href="{{ path("admin.users") }}">Пользователи</a></li>
                <li><a href="{{ path("logout") }}">Выход</a></li>
            </ul>
        </td>
        <td valign="top" class="body-td">
            {% block body %}
            {% endblock %}
        </td>
    </tr>
</table>
</body>
</html>