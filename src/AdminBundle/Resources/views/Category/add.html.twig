{% extends "AdminBundle::layout.html.twig" %}

{% block body %}
    <h2>Добавление категории</h2>
    <form method="POST" action="{{ path("admin.category.add") }}" class="form-horizontal">
        {{ form_widget(form) }}

        <div class="form-group">
            <div class="col-sm-10 col-sm-offset-2">
                <input type="submit" class="btn btn-success" value="Сохранить" />
            </div>
        </div>
    </form>

    {{ render(controller("AdminBundle:Category:categoriesForTreeJson")) }}

{% endblock %}
