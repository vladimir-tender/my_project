{% extends "AdminBundle::layout.html.twig" %}


{% block body %}
    <a href="{{ path("admin.page_list") }}">Список страниц</a>
    <form action="{{ path("admin.page_add") }}" method="post" class="form-horizontal">
        {{ form_widget(form) }}

        <div class="form-group">
            <div class="col-sm-10 col-sm-offset-2">
                <input type="submit" class="btn btn-success" value="Добавить" />
            </div>
        </div>
    </form>

{% endblock %}
